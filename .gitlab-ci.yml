# 驗證 Runner 的 MinIO 快取後端是否生效（test/build 這兩個沒有 rules: changes:，
# 每次都會跑，用來測 Gradle/npm 的快取有沒有真的存到/讀回 MinIO）

# 需要的 CI/CD 變數（GitLab project Settings > CI/CD > Variables）：
#   K8S_REPO_URL          - k8s repo 的 clone URL（含認證，或搭配 deploy token）
#   K8S_REPO_DEPLOY_TOKEN - 有 write 權限的 GitLab deploy token / PAT
# CI_REGISTRY / CI_REGISTRY_USER / CI_REGISTRY_PASSWORD / CI_REGISTRY_IMAGE 由 GitLab 自動注入（見 architecture.md D9）。

stages:
  - test
  - build
  - package
  - deploy

# Runner 只接有打 "k8s" tag 的 job（見 runner 設定 run_untagged: false），
# 用 default 讓所有 job 統一繼承，不用每個 job 各自重複
default:
  tags:
    - k8s

variables:
  GRADLE_OPTS: "-Dorg.gradle.daemon=false"

.gradle-cache:
  cache:
    key: gradle-cache
    paths:
      - .gradle/wrapper
      - .gradle/caches

test:
  stage: test
  image: docker.io/library/eclipse-temurin:21-jdk
  extends: .gradle-cache
  script:
    # 部分測試靠 Testcontainers 起真的 Postgres（見各測試類別的 @Tag("requires-docker")），
    # 但 Runner 用 Kubernetes executor 執行 job（container runtime 是 containerd，job pod 內
    # 沒有 Docker daemon 可用），這些測試會直接連不上 Docker 失敗。要支援需要幫 Runner 開
    # Docker-in-Docker + privileged 模式（影響這個共用 Runner 的安全邊界，不只 job-radar 一個
    # 專案的事），目前先跳過，這些測試仍在本機（有 Docker）手動跑。
    - ./gradlew test -PskipDockerTests --console=plain
  artifacts:
    when: always
    reports:
      junit:
        - "*/build/test-results/test/TEST-*.xml"

build:
  stage: build
  image: docker.io/library/eclipse-temurin:21-jdk
  extends: .gradle-cache
  script:
    - ./gradlew :collector:bootJar :worker:bootJar :api:bootJar --console=plain
  artifacts:
    paths:
      - collector/build/libs/*.jar
      - worker/build/libs/*.jar
      - api/build/libs/*.jar
    expire_in: 1 hour

build:frontend:
  stage: build
  image: docker.io/library/node:20-alpine
  rules:
    - changes:
        - "frontend/**/*"
  cache:
    key: npm-cache
    paths:
      - frontend/node_modules
  script:
    - cd frontend
    - npm ci
    - npm run build
  artifacts:
    paths:
      - frontend/dist/
    expire_in: 1 hour

.package:
  stage: package
  image:
    name: gcr.io/kaniko-project/executor:v1.23.2-debug
    entrypoint: [""]
  needs: ["build"]
  script:
    - mkdir -p /kaniko/.docker
    - >
      echo "{\"auths\":{\"$CI_REGISTRY\":{\"auth\":\"$(printf '%s:%s' "$CI_REGISTRY_USER" "$CI_REGISTRY_PASSWORD" | base64 | tr -d '\n')\"}}}"
      > /kaniko/.docker/config.json
    # registry.192.168.100.200.nip.io 是 homelab 自己簽的內部 PKI（見 architecture.md），
    # kaniko 這個 image 沒有這張 Root CA，直接推會 x509 驗證失敗。用
    # --registry-certificate 明確告訴 kaniko 這個 host 要信哪張憑證（$HOMELAB_CA_CERT 是
    # CI/CD file 類型變數，值就是 Root CA 的 PEM）。
    - >
      /kaniko/executor
      --context "$CI_PROJECT_DIR"
      --dockerfile "$CI_PROJECT_DIR/$SERVICE/Dockerfile"
      --destination "$CI_REGISTRY_IMAGE/$SERVICE:$CI_COMMIT_SHORT_SHA"
      --destination "$CI_REGISTRY_IMAGE/$SERVICE:latest"
      --registry-certificate "$CI_REGISTRY=$HOMELAB_CA_CERT"

package:collector:
  extends: .package
  rules:
    - changes:
        - "common/**/*"
        - "collector/**/*"
  variables:
    SERVICE: collector

package:worker:
  extends: .package
  rules:
    - changes:
        - "common/**/*"
        - "worker/**/*"
  variables:
    SERVICE: worker

package:api:
  extends: .package
  rules:
    - changes:
        - "common/**/*"
        - "api/**/*"
  variables:
    SERVICE: api

package:frontend:
  extends: .package
  needs: ["build:frontend"]
  rules:
    - changes:
        - "frontend/**/*"
  variables:
    SERVICE: frontend

deploy:update-image-tags:
  stage: deploy
  image: docker.io/alpine/git:latest
  # 一旦 job 用 needs: 明確列出依賴，GitLab 就不再照 stage 順序等，只看 needs
  # 清單裡的東西是否都完成。這次四個 package job 都可能因為 rules: changes:
  # 沒符合而整個不存在於這條 pipeline（不是「跑了但被跳過」，是根本沒被排進去），
  # optional: true 讓 deploy 不會因為等不到這種「本來就不存在」的 job 而失敗。
  #
  # 但這樣一來，如果四個 package job 剛好全部都不存在（例如這次只改了
  # .gitlab-ci.yml 本身），deploy 的 needs 清單等於完全沒有東西要等，會立刻執行，
  # 完全不會等 test/build 這兩關——實測抓到這個問題：deploy 在 test 都還沒跑完
  # 的時候就已經先跑完了。明確加上 test（不是 optional，一定要存在且成功），
  # 確保不管 package 那邊有沒有東西要等，deploy 永遠不會搶在測試之前執行。
  needs:
    - job: test
    - job: package:collector
      optional: true
    - job: package:worker
      optional: true
    - job: package:api
      optional: true
    - job: package:frontend
      optional: true
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'
  script:
    # alpine/git 這個 image 一樣不認得 homelab 的內部 CA（見 kaniko 那步的說明），
    # git clone gitlab.192.168.100.200.nip.io 會 x509 驗證失敗，先裝進系統信任庫。
    # 這個 image 沒有預裝 ca-certificates 套件（update-ca-certificates 用到的目錄
    # 不存在），要先裝。
    - apk add --no-cache ca-certificates
    - cp "$HOMELAB_CA_CERT" /usr/local/share/ca-certificates/homelab-root-ca.crt
    - update-ca-certificates
    # package:* job 現在可能因為 rules: changes: 被跳過（代表那個服務的新 image 根本
    # 沒被建置、推上 registry），這裡一定要跟著判斷「這次 commit 到底改了什麼」，只更新
    # 真的有新 image 的服務——不然會把 image tag 指向一個從沒推上去過的版本，
    # 部署下去直接 ImagePullBackOff。判斷邏輯刻意跟前面 package job 的 rules: changes:
    # 用同一套路徑規則，兩邊要保持一致。
    # 這段一定要在 git clone k8s-repo、cd 進去之前算好——cd 過去之後工作目錄就換成
    # 另一個 repo 了，git diff 會對照錯倉庫。
    # 抓不到 before_sha（例如全新分支的第一次 pipeline）時保守地當作全部都要更新，
    # 不要因為判斷不出來就整批漏更新。
    - >
      if git diff --name-only "$CI_COMMIT_BEFORE_SHA" "$CI_COMMIT_SHA" > /tmp/changed_files.txt 2>/dev/null; then
        CHANGED=$(cat /tmp/changed_files.txt);
      else
        echo "抓不到 before_sha，保守起見視為全部服務都變動過";
        CHANGED="common/ collector/ worker/ api/ frontend/";
      fi
    # K8S_REPO_DEPLOY_TOKEN 是 homelab/k8s 專案自己的 Project Access Token（write_repository），
    # 不是 $CI_JOB_TOKEN，所以用 oauth2 當 username，不是 gitlab-ci-token 那個保留字。
    - git clone "https://oauth2:${K8S_REPO_DEPLOY_TOKEN}@${K8S_REPO_URL}" k8s-repo
    - cd k8s-repo/apps/job-radar
    - >
      if echo "$CHANGED" | grep -qE '^(common/|collector/)'; then
        sed -i "s#image: .*/collector:.*#image: ${CI_REGISTRY_IMAGE}/collector:${CI_COMMIT_SHORT_SHA}#" collector.yaml;
        echo "collector.yaml 已更新";
      else
        echo "collector 這次沒變動，跳過";
      fi
    - >
      if echo "$CHANGED" | grep -qE '^(common/|worker/)'; then
        sed -i "s#image: .*/worker:.*#image: ${CI_REGISTRY_IMAGE}/worker:${CI_COMMIT_SHORT_SHA}#" worker.yaml;
        echo "worker.yaml 已更新";
      else
        echo "worker 這次沒變動，跳過";
      fi
    - >
      if echo "$CHANGED" | grep -qE '^(common/|api/)'; then
        sed -i "s#image: .*/api:.*#image: ${CI_REGISTRY_IMAGE}/api:${CI_COMMIT_SHORT_SHA}#" api.yaml;
        echo "api.yaml 已更新";
      else
        echo "api 這次沒變動，跳過";
      fi
    - >
      if echo "$CHANGED" | grep -qE '^frontend/'; then
        sed -i "s#image: .*/frontend:.*#image: ${CI_REGISTRY_IMAGE}/frontend:${CI_COMMIT_SHORT_SHA}#" frontend.yaml;
        echo "frontend.yaml 已更新";
      else
        echo "frontend 這次沒變動，跳過";
      fi
    - git config user.email "ci@job-radar.local"
    - git config user.name "job-radar CI"
    # 有可能四個服務這次都沒變動（純文件 commit 之類的），這種情況 git commit 會因為
    # 沒有任何改動而失敗，用 --allow-empty 讓它照樣留一筆記錄，而不是讓整條 pipeline 失敗
    - 'git commit --allow-empty -am "deploy: job-radar ${CI_COMMIT_SHORT_SHA}"'
    - git push origin HEAD:main
